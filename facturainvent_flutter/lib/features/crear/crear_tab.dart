import 'package:flutter/material.dart';

import 'agregar_xml_screen.dart';

class CrearTab extends StatelessWidget {
  const CrearTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const AgregarXmlScreen();
  }
}
