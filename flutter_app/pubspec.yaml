name: facturainvent2
description: Cliente Windows/Linux/Android de FacturaInvent 2 (mismo backend FastAPI que la app iOS/macOS).
publish_to: "none"
version: 2.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
